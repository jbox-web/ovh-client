---
name: docs

on:
  push:
    branches:
      - master
    paths-ignore:
      - "**.md"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# A single concurrent deployment; cancel in-progress runs.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Git checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Setup mise
        uses: jdx/mise-action@v4
      - name: Install dependencies
        run: mise dev:deps
      - name: Generate API docs (YARD)
        run: mise dev:docs
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./doc
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
